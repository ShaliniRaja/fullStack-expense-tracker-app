// The three buckets a "Send Money" transaction gets tagged with —
// mirrors ALLOCATION values enforced server-side in TransactionService.
export const ALLOCATIONS = ["Cash in Hand", "Investment", "House Expenses"];
