import { Target, Heart, Plane, Laptop, Watch, Home, Car, Palette, Gem, TreePine } from "lucide-react";

export const GOAL_ICONS = {
  shield: Target, heart: Heart, plane: Plane, laptop: Laptop, watch: Watch,
  home: Home, car: Car, palette: Palette, gem: Gem, tree: TreePine,
};

export const GOAL_ICON_KEYS = Object.keys(GOAL_ICONS);
